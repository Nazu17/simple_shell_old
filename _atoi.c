#include <stdio.h>

/**
 * main - entry point of the program.
 *
 * This program prints the message "Stop, war on religion!"
 * to the standard output.
 *
 * Return: always 0 (Success)
 */

int main(void)
{
	char *message = "Stop, war on religion!";

	printf("%s\n", message);

	return (0);
}
